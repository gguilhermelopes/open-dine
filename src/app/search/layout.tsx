export const metadata = {
  title: "Search | Open Dine",
  description: "Open Dine",
};

const SearchLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default SearchLayout;
