export const metadata = {
  title: "Reserve at Terraço Jardins | Open Dine",
  description: "Open Dine",
};

const ReserveLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default ReserveLayout;
